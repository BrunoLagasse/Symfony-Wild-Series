{% extends 'base.html.twig' %}

{% block title %}Actor #{{ actor.id }}{% endblock %}

{% block body %}

    <h1 class="text-center my-4">{{ actor.name }}</h1>
    <h2>Listes des séries</h2>
    <ul>
        {% for program in actor.programs %}
            <li><a href="{{ path("program_show", { programId: program.id }) }}">{{ program.title }}</a></li>
        {% else %}
            Aucune série trouvé
        {% endfor %}
    </ul>
{% endblock %}