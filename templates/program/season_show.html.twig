{% extends 'base.html.twig' %}

{% block title %}Saison {{ seasons.number }}{% endblock %}

{% block body %}

<div class="media">
    <div>
        {% for episode in episodes %} <br>
            <h4><a href="{{ path('program_season_episode_show', {seasonId: seasons.id, programId: program.id, episodeId: episode.id}) }}">Episode {{ episode.number }}</a></h4>
            <b>Titre: </b>{{ episode.title }}<br>
        {% endfor %}
    </div>
</div>
<br>
<a href="{{ path('program_index') }}">Retour à la liste des programmes</a>

{% endblock %}